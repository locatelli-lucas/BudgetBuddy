import styled from "styled-components";
import {fontFamily, mainColor} from "../style.ts";

export const GlobalTitle = styled.h1`
    position: relative;
    font-family: ${fontFamily};
    margin: -1em 0;
    color: ${mainColor};
`