import styled from "styled-components";
import {darkTextColor} from "../../global_styles/style.ts";

export const Subtitle = styled.h2`
    position: relative;
    margin: 0.5em 0 -1em 0;
    color: ${darkTextColor};
`